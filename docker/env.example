# Copy to docker/.env and edit before running `docker compose up -d`.
# These values seed the Postgres container on its FIRST start; changing them
# later does not migrate the existing database.

OMRAT_DB_NAME=omrat
OMRAT_DB_USER=omrat
OMRAT_DB_PASSWORD=omrat
OMRAT_DB_PORT=5432
